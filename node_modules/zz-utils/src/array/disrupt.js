"use strict";
var isArray = require("./isArray");

function disrupt(array) {
    if (!isArray) {
        throw new TypeError("argument 'array' must be type of Array.");
    } else {
        return array.slice().sort(function () {return Math.random() - 0.5});
    }
};

module.exports = disrupt;