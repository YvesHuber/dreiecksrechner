"use strict";

function isPlainObj(obj) {
    return !!obj 
        && typeof obj === "object" 
        && !obj.hasOwnProperty("toString")
        && /\[object\sObject\]/.test(obj.toString());
};

module.exports = isPlainObj;